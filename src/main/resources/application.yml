
spring:
  profiles:
    active: mysql # ??? ??? ??? ???? ????

# ?????? ??
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
      username: sa
      password:
      driver-class-name: org.h2.Driver

---
spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:5431/springbatch?useUnicode=true&characterEncoding=utf8
      username: root
      password: 303ehd
      driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    jdbc:
      initialize-schema: always
#    job:
#      enabled: false




